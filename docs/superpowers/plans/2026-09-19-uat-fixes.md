# Frontend UAT Fix Cycle Implementation Plan

Goal: 修复 UAT-001～005，保持核心业务/API/数据库语义不变。

用户已给出并授权设计；在 fix/uat-v0.1.1 分支内顺序实施。

Architecture: AdminLayout 的页面内容使用共享 CSS pattern 约束直接子 Card 的标题和工具栏；1024标题独占一行，桌面按空间换行。复杂列表通过 table scroll.x 限制在内容区域；资费关键列设最小宽度。AntD 使用其 ESM 中文 locale，dayjs显式设zh-cn。PermRoute渲染轻量Forbidden组件并保留请求URL，未登录守卫保持原样。

Tech stack: React / AntD 5 / Vite production preview / Playwright Chromium。

## Execution

- [x] 从main创建分支，验证v0.1.1祖先与产品内容；带入e8781a8的UAT设施与报告。
- [x] 保存旧截图/trace/JSON至artifacts/uat/fix-before。
- [x] tests/uat/uat-fixes.spec.ts：先写完整标题、工具栏矩形不重叠、资费名称最小宽度与行高、中文空态/日期/月/分页、三个角色Forbidden和未登录区分的回归。运行未修复生产构建并保存RED证据。
- [x] apps/web/src/index.css + layouts/AdminLayout.tsx：共享page-content Card规则；标题不缩小、工具栏wrap、选择框填充既定宽度；1024分行，内容min-width:0。
- [x] Tariffs.tsx：名称和日期minWidth，列表scroll.x；ReadingPlans.tsx与Settlements.tsx设置内部scroll.x。Bills已有内部scroll，保留。
- [x] App.tsx：使用antd/es/locale/zh_CN；dayjs.locale('zh-cn')。新增pages/Forbidden.tsx：403、中文原因、返回工作台。权限模型及菜单过滤不变。
- [x] 调整现有权限test的“跳首页”旧预期为中文Forbidden；Happy Path日期定位改为中文月份，保留全部原65项及业务断言。
- [x] pnpm build；重启vite preview；运行新增回归至绿色，检查before/after截图与网络。
- [x] 仅重建本会话water_uat_v011，显式migrate/seed；生产API和Web下pnpm exec playwright test。目标原65项全通过，新增回归全通过，flaky=0、unexpected错误=0。
- [x] 更新UAT_REPORT的Fix Cycle结果、before/after证据、测试计数和限制。
- [x] 类型/构建/差异校验后单次提交fix(uat): address v0.1.1 frontend UAT findings，推送fix分支；不merge、不tag。

## Scope guard

允许：上述UI、样式、测试与报告。禁止：apps/api、packages业务逻辑、prisma schema/migrations和无关重构。实际测试业务请求始终通过4173/api代理；布局fixture仅用于独立回归前置数据。
